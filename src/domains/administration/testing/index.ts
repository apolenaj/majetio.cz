/**
 * Admin Ops test kit — fixtures (250) + unit/E2E suites (251–272).
 */

export {
  buildAdminOpsFixtures,
  fixtureAttentionSignals,
  FIXTURE_NOW,
  type AdminOpsFixtureBundle,
} from "./admin-ops-fixtures";
