# Scope (B) — Rozsah rekonstrukce

Defines **what** will be renovated. No unit rates, no totals, no ARV.
Versioned via `scopeVersion` on `RenovationAnalysis`.

- `inferScopeFromCondition()` — automatic inference from condition assessment
- `applyUserScopeOverrides()` — manual edits with frozen automatic snapshot
- Standards: cosmetic → premium (+ custom)
- Version: `scope.v2026.07`
