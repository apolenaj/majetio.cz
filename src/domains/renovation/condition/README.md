# Condition (A) — Současný stav

Physical / technical condition **before** works.
Outputs `RenovationConditionAssessment` with per-area statuses — never Kč amounts or ARV.

- `assessCondition()` — maps `Property.condition` → 9 assessed areas
- Structure stays `unknown` unless inspection override
- Version: `condition.v2026.07`
