# Domain: investment-calculations

Legacy lightweight helpers (`calculateGrossYield`, `calculateMonthlyCashFlow`).

**New engine foundation:** `src/domains/investment/engine/`  
Prefer the new domain for schemas, Money DTOs, formula registry, and acquisition cost.

This package remains until callers are migrated.
