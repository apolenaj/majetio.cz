import { MobileBottomNavigation as BaseMobileNav } from "@/components/navigation/mobile-nav";

/** Account-scoped bottom nav — uses shared component from Prompt 3. */
export function MobileBottomNavigation() {
  return <BaseMobileNav />;
}
