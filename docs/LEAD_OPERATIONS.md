# Lead Operations

Admin lead queues for marketplace / mortgage / QBL ops. Permissions: `leads.read` / `leads.write`.

## Surface

`/admin/leady` — operational queues (not public ranking).

## Rules

1. No public broker leaderboard / ranking UI in admin or Profi inbox.
2. PII handled per lead privacy rules — mask on admin search cards.
3. SLA / response tooling stays assignee-scoped (see CRM docs).

## Related

`docs/MARKETPLACE_LEADS.md`, `docs/QUALIFIED_BUYER_LEADS.md`, `docs/LEAD_BILLING.md`, `docs/CRM_ARCHITECTURE.md`
